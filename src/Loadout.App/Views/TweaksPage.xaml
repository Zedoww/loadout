<Page x:Class="Loadout.App.Views.TweaksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Tweaks">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type Page}}}">
        <Grid Margin="28,24,28,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,18">
                <TextBlock Text="Tweaks" Style="{StaticResource Type.PageTitle}" />
                <TextBlock Text="Well-known Windows optimizations. Each one saves its original value and can be reverted at any time."
                           Style="{StaticResource Type.PageSubtitle}" Margin="0,4,0,0" />
            </StackPanel>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,8">
                            <TextBlock Text="{Binding Name}" Margin="2,12,0,8"
                                       FontFamily="{StaticResource Font.Display}"
                                       FontSize="12.5" FontWeight="Bold"
                                       Foreground="{StaticResource Brand.Primary}" />

                            <ItemsControl ItemsSource="{Binding Tweaks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource Surface.Row}" Padding="16,13" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                                   FontSize="14" Foreground="{StaticResource Text.Primary}" />
                                                        <Border Style="{StaticResource Pill}" Margin="10,0,0,0"
                                                                Background="#3A2E14"
                                                                Visibility="{Binding RequiresReboot, Converter={StaticResource BoolToVisibility}}">
                                                            <TextBlock Text="reboot" FontSize="10.5"
                                                                       Foreground="{StaticResource Warning}" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Description}" Style="{StaticResource Type.Body}"
                                                               Margin="0,3,0,0" />
                                                    <TextBlock Text="{Binding Status}" FontSize="12" Margin="0,3,0,0"
                                                               Foreground="{StaticResource Success}" />
                                                </StackPanel>

                                                <ui:Button Grid.Column="1" VerticalAlignment="Center" MinWidth="104"
                                                           Content="{Binding ButtonText}"
                                                           Command="{Binding ToggleCommand}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</Page>
