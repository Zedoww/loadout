<Page x:Class="Loadout.App.Views.SurgePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Surge">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Surge" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Optimisations réversibles : plan d'alimentation performance + libération mémoire."
                       Opacity="0.7" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <ui:Card Grid.Row="1" Padding="28" Margin="0,0,0,16">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           FontSize="15" Margin="0,0,0,20" />
                <ui:Button Command="{Binding ToggleCommand}"
                           Appearance="Primary"
                           Padding="40,16"
                           FontSize="16">
                    <ui:Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Rocket24" Margin="0,0,10,0" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding ButtonText}" />
                        </StackPanel>
                    </ui:Button.Content>
                </ui:Button>
            </StackPanel>
        </ui:Card>

        <ui:Card Grid.Row="2" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Log}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="0,3" FontFamily="Cascadia Mono, Consolas" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </ui:Card>
    </Grid>
</Page>
