<Page x:Class="Loadout.App.Views.SurgePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Surge">

    <Page.Resources>
        <Style x:Key="SurgeButton" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="{StaticResource Font.Display}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="16" Padding="46,18"
                                Background="{StaticResource Brand.Gradient}">
                            <Border.Effect>
                                <DropShadowEffect Color="#7C6CFF" BlurRadius="30" ShadowDepth="0" Opacity="0.5" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.82" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FeatureChip" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Background" Value="#161B27" />
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type Page}}}">
        <Grid Margin="28,24,28,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Surge" Style="{StaticResource Type.PageTitle}" />
                <TextBlock Text="One-click performance mode — reversible. High-performance power plan, freed memory, paused background apps."
                           Style="{StaticResource Type.PageSubtitle}" Margin="0,4,0,0" />
            </StackPanel>

            <ui:Card Grid.Row="1" Padding="32" Margin="0,0,0,16">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap"
                               HorizontalAlignment="Center" TextAlignment="Center"
                               Style="{StaticResource Type.Body}" FontSize="14" Margin="0,0,0,22" />

                    <Button Command="{Binding ToggleCommand}" Style="{StaticResource SurgeButton}"
                            HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Rocket24" Margin="0,0,10,0" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding ButtonText}" />
                        </StackPanel>
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                        <Border Style="{StaticResource FeatureChip}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Flash24" FontSize="15" Foreground="{StaticResource Brand.Primary}" />
                                <TextBlock Text="High-performance plan" Margin="7,0,0,0" Style="{StaticResource Type.Body}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource FeatureChip}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Ram20" FontSize="15" Foreground="{StaticResource Brand.Primary}" />
                                <TextBlock Text="Free memory" Margin="7,0,0,0" Style="{StaticResource Type.Body}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource FeatureChip}" Margin="0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Pause24" FontSize="15" Foreground="{StaticResource Brand.Primary}" />
                                <TextBlock Text="Pause background apps" Margin="7,0,0,0" Style="{StaticResource Type.Body}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <Border Grid.Row="2" Style="{StaticResource Surface}" Padding="16" MinHeight="120">
                <ItemsControl ItemsSource="{Binding Log}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="0,3"
                                       FontFamily="{StaticResource Font.Mono}" FontSize="12.5"
                                       Foreground="{StaticResource Text.Secondary}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
